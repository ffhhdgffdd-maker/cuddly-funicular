# WolFox — مراجعة تنظيم النسخ والسلوك

الاسم الظاهر داخل التطبيق WolFox فقط. الإصدار والفرع والبناء يحددان النسخة خارج الواجهة.

التعديلات الفعلية تشمل:
- إزالة زر الإعدادات العام والقائمة العامة المخفية القديمة. إعدادات الموقع في الموقع، والكاميرا في الكاميرا، وBluetooth في قسمه، والجدولة داخل نافذتها، والتفعيل وتنبيه الاشتراك تحت التاج.
- إزالة أيقونة حالة ECG الخضراء بجانب التاج فقط، مع إبقاء التاج وحالات التشغيل الأخرى.
- إدارة بحث Bluetooth فعلية بجيل مستقل لكل بحث، مع إيقاف البحث وإعادة البحث، مهلة الإذن، معالجة تغير حالة الجهاز، وتحديث النتائج دون فقد بيانات الإعلان السابقة.
- حفظ ملفات Bluetooth والتحقق منها وقراءتها وكتابتها، مع نسخ مستقلة للملفات وحماية التزامن. تمرير استدعاءات التطبيق الأصلية وعدم إسقاط نتائج الأجهزة الأخرى.
- أيقونة WolFox تفتح كامل الواجهة. نافذة وسط الشاشة تحفظ طريقة الاستعادة (الأيقونة، الصوت، كلاهما) وتؤكد الحفظ قبل الإخفاء. لا يُقتل التطبيق المضيف بسبب الحفظ أو إغلاق الواجهة.
- زر الكاميرا يبدل تشغيل الميزة دون فتح الأيقونة أثناء التصفح. دورة الأيقونة مرتبطة بمعاينة كاميرا ظاهرة ونشاط اختيار الصورة والالتقاط والرفع.

## نطاق الكاميرا والتحقق

التنفيذ يراقب معاينة AVFoundation الظاهرة (`AVCaptureVideoPreviewLayer`)، ونداءات `AVCapturePhotoOutput`، ورفع الصورة المختارة عبر مهام `NSURLSessionUploadTask` من بيانات/ملف. لا يصطنع نجاح التقاط أو رفع ولا يغير نداءات إكمال المضيف.

الكاميرات التي ترسم عبر Texture مخصص دون طبقة معاينة AVFoundation، أو الرفع الذي يعيد ترميز الصورة أو يستخدم مكتبة مختلفة، يحتاج مراجعة تكامل مع مصدر التطبيق المضيف. لا توجد نسخة التطبيق المضيف أو جهاز متصل هنا لإثبات هذه المسارات.

جميع الوظائف **غير مختبرة على جهاز حقيقي**. الاختبارات الأصلية تختبر محللات الملفات ومنطق البحث وتمرير delegate والحفظ ودورة الكاميرا، والبناء يثبت قابلية ترجمة وربط الكود فقط. اختبارات Linux المحاكية لا تمثل اختبار أجهزة Bluetooth أو كاميرا iPhone.

## أذونات المضيف

هذا المشروع ينتج dylib وحزم DEB، وليس تطبيق IPA مستقلًا. وصف إذن Bluetooth والموقع يجب أن يكون في Info.plist للتطبيق المضيف. لا يكفي وضعه في فلتر حقن WolFox.

يرفض البحث عند غياب وصف Bluetooth بدل تشغيل طلب غير صالح، ويُطلب إذن الموقع عند اختيار المستخدم عرض موقعه. منتقي الصور PHPicker مخصص للاختيار الذي يجريه المستخدم. تذكير الاشتراك يطلب موافقة الإشعارات ويعرض الرفض.

الأداة `tools/prepare_host_privacy.py` تجهز نسخة مستقلة من Info.plist للتطبيق المصرح بتعديله، وتحافظ على الأوصاف الموجودة، وتضيف الأوصاف الناقصة فقط. لم تُعدل أذونات تطبيق مضيف لعدم وجود حزمة المضيف ضمن المدخلات. يلزم تضمين الناتج وإعادة توقيع تلك الحزمة ضمن مسار البناء المصرح به.

## البناء والفروع

لا دمج إلى main. كل فرع إصدار يملك release.json وWorkflow للبناء خاصًا بالفرع. يُرفق المصدر المطابق وCOMMIT وRUN وبصمات SHA256 مع كل بناء ناجح. الفروع القديمة محفوظة للمراجعة؛ فروع الألوان تستعيد ملفات الألوان الفعلية من سجل المشروع.
