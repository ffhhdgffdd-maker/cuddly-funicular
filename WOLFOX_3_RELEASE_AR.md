# WolFox 3.0.0 — الإصدارات الأربعة

كل بناء يستهدف تطبيقًا واحدًا؛ لا تستخدم حزمة Tahakom في تطبيق منسوبي المساجد أو العكس.

| الإصدار | اسم البناء | التطبيق |
| --- | --- | --- |
| 1. Full تحكم | control-full | com.tahakom.mytahakom |
| 2. Full مساجد | mosques-full | sa.gov.moia.mosques-2 |
| 3. Lite | lite-tahakom / lite-mosques | حزمة مستقلة لكل تطبيق |
| 4. Full | full-tahakom / full-mosques | حزمة Full مستقلة لكل تطبيق |

تنتج أربعة تسميات إصدار ستة ملفات تعريف، ولكل ملف تعريف حزمة Rootful وأخرى Rootless وملف dylib. تشترك نسخ Full في الوظائف الأساسية الحالية؛ النسخة الرابعة تفصل التغليف والاستهداف وتسميتها عن النسختين المخصصتين. يجب تثبيت ملف تعريف واحد فقط لكل تطبيق.

اسم الواجهة والحزمة الظاهر للمستخدم موحّد: WolFox. تظل أسماء الملفات ومعرّفات الحزم منفصلة لاختيار التطبيق والنسخة الصحيحين.

## التصميم

لكل إصدار هوية مرئية مستقلة: تحكم أزرق، مساجد فيروزي، Lite بلون دافئ، وFull بنفسجي. تتغير الترويسة وشكل شريط الأقسام وعنوان الإصدار تبعًا لملف البناء، وتبقى الأقسام والوظائف المتاحة مرتبطة بنسخة Full أو Lite. يظل ربط معرّف التطبيق مستقلًا عن التصميم. يحتفظ لون النجاح الأخضر والخطر الأحمر بمعناهما.

## الربط

يحدد البناء WOLFOX_TARGET_BUNDLE_IDS وWOLFOX_PROJECT_BUNDLE_ID بالمعرّف نفسه لكل ملف تعريف ويرفض أي تعارض. رابط API: https://gps.p3nd.fun/api/v1. في GitHub Actions يُقرأ مفتاح التطبيق من WOLFOX_PROJECT_KEY_TAHAKOM أو WOLFOX_PROJECT_KEY_MOSQUES؛ عند عدم ضبطه يستخدم المفتاح القديم WOLFOX_PROJECT_KEY، وقد يلزم مفتاح مشروع منفصل لقبول خادم الترخيص. قيمة المفتاح لا تُضاف إلى المستودع.

## استيراد معرّف منسوبي المساجد

حقل المعرّف يبدأ فارغًا لإدخال UUID يدويًا. بعد «حفظ وتفعيل» تظهر علامة زرقاء وحالة التفعيل وسيريال UUID المحفوظ؛ زر «تصدير» ينسخ السيريال الفعّال، أو UUID صحيحًا أدخله المستخدم، ويرفض التصدير إذا لم توجد قيمة صالحة. في صفحة «المعرّف وUDID» لتطبيق `sa.gov.moia.mosques-2` يظهر زر «استيراد معرّف تطبيق المساجد». يقرأ قيمة `flutter_udid` من Keychain للتطبيق الحالي فقط، ويتحقق أنها UUID صالح ثم يضعها في حقل المراجعة؛ الضغط على «حفظ وتفعيل» هو الذي يربطها بالتطبيق. عند غياب القيمة أو تعذر قراءتها تظهر رسالة واضحة، ويظل الاستيراد اليدوي من الحافظة متاحًا. لا يقرأ البناء معرّف تطبيق آخر ولا ينشئ قيمة مزيفة بدلًا من قيمة التطبيق.

## الخريطة

نُقل البحث العام إلى الشريط فوق الخريطة، وأزيلت أزرار تحريك 5 و10 أمتار. يظهر حفظ الموقع بالأخضر والمفضلة بالبرتقالي، وتتميز أزرار الخريطة الأخرى بألوان مختلفة. لا تعرض الخريطة النشطة مدارس أو مساجد أو خدمات صحية كطبقة خاصة، ولا تبدأ طلبات تحميلها. يظل معرّف حزمة تطبيق منسوبي المساجد ثابتًا لأغراض الربط فقط.

## التحقق والتثبيت

يجري سير build-v3.yml الاختبارات والبناء ويتحقق من إصدار DEB وفلتر التطبيق. يبدأ نطاق WolFox من iOS 15.8 على arm64، بصرف النظر عن الحد الأدنى لكل تطبيق. يحتوي ملفا IPA اللذان رُفعا على K7GPS مدمج؛ لا يزيله البناء ولا يعدل ملفي IPA. لم يُختبر اجتماع K7GPS وWolFox أو نجاح الترخيص وواجهة التصميم على جهاز فعلي. استخدم جهاز اختبار وملف تعريف واحدًا في كل مرة قبل الاعتماد.
